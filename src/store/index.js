import { createStore } from "vuex";

const config = "https://ifh.cc/g/";

export default createStore({
  state: {
    urlConfig: config,
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
